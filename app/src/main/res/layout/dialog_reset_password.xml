<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@color/container">

    <RelativeLayout
        android:layout_width="500dp"
        android:layout_height="wrap_content"
        android:padding="30dp">

        <EditText
            android:id="@+id/edtMail_resetPasswordDialog"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:layout_marginStart="5dp"
            android:layout_marginTop="5dp"
            android:layout_marginEnd="5dp"
            android:layout_marginBottom="5dp"
            android:background="@drawable/edit_text_custom"
            android:drawableStart="@drawable/email_icon_custom"
            android:drawablePadding="3dp"
            android:hint="email"
            android:inputType="textEmailAddress"
            android:paddingVertical="8dp"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:textColor="@color/on_container_text_color"
            android:textColorHint="@color/on_container_text_color" />

        <Button
            android:id="@+id/btnSendMail_resetPasswordDialog"
            style="@style/buttonInContainer"
            android:layout_width="300dp"
            android:layout_below="@+id/edtMail_resetPasswordDialog"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="10dp"
            android:text="send reset email" />

    </RelativeLayout>

</LinearLayout>